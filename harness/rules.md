# Harness rules

Appended to the agent system prompt by the harness agent variant.
BASELINE = this file empty of rules. The fixer loop edits ONLY this file.

<!-- No rules yet. Baseline runs with stock llm_agent (this file unused). -->
